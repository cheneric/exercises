package cheneric.exercise.phonenumberwords;

/**
 * Validates strings as valid words.
 */
public class Dictionary {
	/**
	 * Indicates whether the input <code>word</code> is valid.
	 * 
	 * @param word the word to test.
	 * @return <code>true</code> if the input <code>word</code> 
	 * is valid; otherwise, returns <code>false</code>.
	 */
	public boolean isWord(String word) {
		return true;
	}
}
